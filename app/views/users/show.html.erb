<div>
  <h1><%= current_user.first_name %></h1>
  <!-- show user information -->
  <%= link_to "Edit user registration path", edit_user_registration_path %>
</div>

<div data-controller="navbar">
  <div class="navbar-show">
    <ul>
      <li data-target="navbar.ticketsToggle" class="navbar-show-item">
        Tickets
      </li>
      <li data-target="navbar.historyToggle" class="navbar-show-item">
        History
      </li>
    </ul>
  </div>
  <div data-target="navbar.history" style="display: none;">
    <ul>
      <% @events.each do |event| %>
        <li>
          <%= tag.a(href: event.link) do%>
            <%= event.description %> &nbsp;
            <%= event.created_at %>
          <% end %>
        </li>
      <% end %>
    </ul>
  </div>
  XXXXXXXXXXXXX
  <div data-target="navbar.tickets" style="display: block;">
    <ul>
      <% @user_tickets.each do |ticket|%>
        <li>
          <%= link_to ticket.title, ticket_path(ticket) %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
<%= link_to "<< go back", root_path, class: "link-navigation" %>
